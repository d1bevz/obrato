[package]
name = "compute-core"
version = "0.1.0"
edition = "2024"
description = "Obrato compute-core: детерминированный движок прораба — модель ввода → нормы (диапазон+confidence) → количества. Контракт: гл.05 §8, формулы: гл.07. Ноль зависимостей сверх std (слайс 0, гл.11)."

[dependencies]
# Намеренно пусто (гл.11 слайс 0: «ноль зависимостей сверх std»).
# serde/tsify/wasm-bindgen появляются на слайсе 3 (WASM-граница, гл.09 §5).
