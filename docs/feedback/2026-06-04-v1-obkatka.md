# Обкатка v1 (P0–P5) — комментарии Димы, triage и статусы

> Процесс: Дима вываливает комментарии в любом виде → Архимед раскладывает
> сюда с категорией и приоритетом → пачки-итерации (каждая заканчивается
> задеплоенным артефактом + ревью) → статус обновляется коммитом.
>
> Деплой первой версии: 2026-06-04, https://72.61.103.206.sslip.io
> (PR #5, merge c575883).

## Категории

- **bug** — сломано/врёт: чинится в первую очередь
- **ux** — непонятно/неудобно: мелочь пачкой, крупное отдельной итерацией
- **norm/data** — нормы, каталог, цены: трек калибровки (гл.11), не код
- **design** — меняет дизайн-решения: сперва правка docs/design, потом код
- **deferred** — фаза 2: паркуется со ссылкой на главу

## Статусы

`new` → `triaged(P0|P1|P2)` → `in-progress` → `done(commit)` / `parked(ссылка)`

---

## Бэклог

| # | Комментарий | Категория | Прио | Статус | Резолюция |
|---|---|---|---|---|---|
| 1 | Персонажа (Amigo) нет нигде — а он прописан в гл.08 §10 с готовыми SVG-ассетами (`08-mascot/`) | ux | P1 | done(итерация 1) | Amigo.tsx (символы из `08-mascot/index.html` as is); марка в OfflineBadge на каждом экране («офлайн = без усов»), S1 empty-state «Какой объект сегодня?», S5 freeze-успех (эфемерный — back/reload не повторяет). 2-я очередь (tip S3 «неопределённость», «считаю…», favicon, анимации) — отложена мелочью |
| 2 | Нет визуализации плана дизайн-проекта | design | P1 | done(итерация 2) | решение «Оба» → **D12** (гл.04): (a) план-картинка — страницы реального PDF дизайн-проекта для демо (бандл+precache, офлайн) + фото/скан для своих объектов (Blob в IndexedDB, даунскейл 2560px webp), лайтбокс; (b) схема-карта комнат по габаритам с honesty-баннером «не план квартиры», тап → редактор. На S2 ПОСЛЕ комнат (план — референс, не рабочий объект) |
| 3a | Лист покупок не даёт ссылок на товары | bug | P0 | done(итерация 1) | название SKU = ссылка на товар (продуктовые url), магазин+упаковка+цена своей строкой; плейсхолдерные url (главная/категория) и SKU без url честно ведут в поиск с меткой «поиск». Магазин в снапшоте — frozen by value (skuStores, находка ревью) |
| 3b | Лист не даёт выбрать (альтернативные SKU/магазины) | ux | P1 | done(итерация 1) | read-only «ещё N вариантов в магазинах» в живом листе: цена/уп + магазин + ссылка, сортировка по цене за base_unit; расчёт не трогает (CATALOG_INPUT default-only). Персистентная подмена SKU = PurchaseLineSelection — deferred (05:429, 08:367) |
| 3c | Лист не даёт «реально купить» | design | P2 | done(итерация 1) + parked | покупки внутри app нет в видении нигде (ни корзины, ни checkout): модель пилота = ссылка наружу → freeze → факт S6 (петля калибровки). Микрокопи в notice S5 объясняет модель; in-app покупка — parked (не в видении, триггера нет) |
| 3d | (из ревью 3a) часть url в seed — плейсхолдеры: 5 SKU без url (в т.ч. default grout!), ~14 — главная/категория магазина | norm-data | P2 | triaged | UI размечает их «поиск» и не выдаёт за товар (коммит итерации 1); докурировать реальные товарные url руками при следующей ре-курации каталога (D10) |
| 2b | «Почему не полноценная планировка?» — нужно реальное расположение комнат | design | P1 | done(итерация 3) | в данных нет топологии; полный редактор отвергнут (фаза 2+). **D12c**: режим «✋ расставить» — drag комнат (snap 10 см), тап/Enter = поворот 90°, пересечения подсвечены, позиции в PlacementDoc (презентационный слой, ядро не тронуто); запись только после реального действия — баннер честности не врёт |

---

## Журнал итераций доработок

### Итерация 1 — ✅ закрыты #1, #3a, #3b, #3c (2026-06-05)
- Лист: ссылка на товар + магазин/упаковка/цена своей строкой; read-only
  «ещё N вариантов» (цена+магазин+ссылка, сорт по цене за base_unit), расчёт
  не тронут; микрокопи про модель покупки (ссылка → магазин → freeze → факт).
- Amigo: компонент из готовых SVG-символов `08-mascot/index.html` —
  марка в шапке (офлайн = без усов), S1 empty-state, S5 freeze-успех.
- Ревью (адверсариальное, 4 измерения × verify): 18 заявок → 3 подтверждено
  (5 минус дубли) → исправлено до коммита: (а) storeName в снапшоте брался из
  живого каталога — заморожен (SnapshotDoc.skuStores by value, старые снапшоты
  живут без магазина, не врут); (б) плейсхолдерные url (главная/категория,
  гл.10 §10) выдавались за товарные — размечены, ведут в поиск; (в) 5 SKU без
  ключа url (включая default grout) — тип честно `url?`, в данных явный null,
  докурация реальных url → #3d (norm-data).
- QA: headless-браузер на свежих портах (SW-кэш), мобильный viewport 390×844,
  печать через pdf (маскот/раскрывашки не попадают), офлайн-симуляция
  (усы пропадают), эфемерность freeze-фанфар (reload/back — тихо).
- Тесты: cargo 46 ✓, node (frozen TS prototype, из-за правки seed) 24 ✓, tsc ✓.

### Итерация 2 — ✅ закрыт #2 (2026-06-05)
- D12 записан в гл.04; реализация: ObjectPlan (план-картинка: бандл-страницы
  PDF для демо + фото/скан Blob'ом, webp-даунскейл, лайтбокс) + RoomMap
  (схема-карта: shelf-packing по габаритам, wet голубым, тап → редактор).
- DB v5: store `plans` + бэкфилл planAssets в уже посеянное демо
  (async-upgrade, единый источник — фикстура).
- Ревью (4 измерения × verify): 21 заявка → 8 подтверждено → исправлено до
  коммита: бэкфилл из fire-and-forget стал await'нутым (полу-апгрейд ронял бы
  openDB молча); dbPromise сбрасывается при reject (private mode не залипает);
  honesty-баннер схемы сделан заметным (D12: «пометка обязательна»); план/схема
  перенесены ПОД комнаты (не топить основной флоу S2); webp 0.9 / jpeg 0.92 и
  потолок 2560px (jpeg 0.85 мылил штриховой чертёж); Escape+scroll-lock
  лайтбокса; decoding=async; дедуп хардкода фикстуры.
- QA: миграция v4→v5 прогнана честно (служебная страница собрала прод-образ
  v4-базы → апгрейд → planAssets на месте); загрузка 4961px PNG → 2560px webp
  93KB в IDB; «мой план» переживает reload; «убрать» работает; пустой проект —
  подсказка; схема скрыта при 0 комнат.

### Итерация 3 — ✅ закрыт #2b (2026-06-05): D12c ручная расстановка
- RoomMap: режим «✋ расставить» (drag + snap 10 см + clamp в холст), тап/Enter
  = поворот 90°, пересечения красным пунктиром (не запрещены), персист на
  pointerup/cancel; DB v6 (store placements), deleteRoom чистит позицию.
- Ревью (3 измерения × verify): 17 заявок → 12 подтверждено (10 уникальных) →
  исправлено до коммита: persist/capture не зависят от editing («готово»
  между down и up не теряет позицию); порог тап/драг в экранных px, не в
  метрах схемы (на большой квартире поворот был бы недостижим); pointercancel
  сохраняет подвинутое; «зашёл-вышел» не пишет PlacementDoc — баннер
  «расставлено вручную» не врёт (honesty D12); поворот клампится в холст;
  каскад .wet>.overlap чинён (пересечение мокрой комнаты было синим);
  пояснение пунктира и в view-режиме; aria-pressed + Enter-поворот
  (клавиатура не глотается); LWW-ограничение зафиксировано в D12c.
- QA: drag/тап-поворот/клавиатура с персистом, 5px=тап, reload, переживание
  «зашёл-вышел», навигация тапом в view-режиме.
